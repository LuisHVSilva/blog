import { useMemo, useState, type ReactNode } from "react";
import type { DemoItem } from "../../../content/demo-content";
export function PlaceholderNotice() { return <p className="placeholder-notice" role="note"><strong>Conteúdo fictício:</strong> esta área é estrutural. Substitua os exemplos antes de publicar.</p>; }
export function DemoCard({ item, action = "Ver estrutura" }: { readonly item: DemoItem; readonly action?: string }) { return <article className="demo-card"><span className="placeholder-badge">FICTÍCIO</span><p className="demo-card__meta">{item.meta}</p><h2>{item.title}</h2><p>{item.description}</p><div className="demo-card__tags">{item.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><a className="demo-card__action" href="#top">{action} →</a></article>; }
export function DemoFilters({ items, children }: { readonly items: readonly DemoItem[]; readonly children: (items: readonly DemoItem[]) => ReactNode }) { const [query, setQuery] = useState(""); const visible = useMemo(() => items.filter((item) => `${item.title} ${item.description} ${item.tags.join(" ")}`.toLowerCase().includes(query.toLowerCase())), [items, query]); return <><label className="demo-filter"><span>Buscar nesta página</span><input onChange={(event) => setQuery(event.target.value)} placeholder="Filtrar conteúdo fictício" type="search" value={query} /></label>{children(visible)}</>; }
